services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: market_impact
    ports:
      - "5432:5432"
    volumes:
      - market_impact_pg:/var/lib/postgresql/data

volumes:
  market_impact_pg:
